name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_call:
    inputs:
      environment:
        description: Nome do ambiente alvo (apenas rotulagem nos logs)
        type: string
        required: false
        default: ci

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # Gate explícito dos deploys QA/prod (via workflow_call): unitários SaaS
  # rodam sem heroku-postbuild (export Expo / nest build completo).
  unit-saas-backend:
    name: unit saas-backend (${{ inputs.environment || 'ci' }})
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: postgresql://ci:ci@localhost:5432/ci
      DIRECT_URL: postgresql://ci:ci@localhost:5432/ci
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22.x
          cache: npm
          cache-dependency-path: saas/backend/package-lock.json

      - name: Instalar dependências
        run: npm ci
        working-directory: saas/backend

      - name: Prisma generate
        run: npx prisma generate --generator client
        working-directory: saas/backend

      - name: Testes unitários
        run: npm test
        working-directory: saas/backend

  unit-saas-frontend:
    name: unit saas-frontend (${{ inputs.environment || 'ci' }})
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22.x
          cache: npm
          cache-dependency-path: saas/frontend/package-lock.json

      - name: Instalar dependências
        run: npm ci
        working-directory: saas/frontend

      - name: Testes unitários
        run: npm test
        working-directory: saas/frontend

  build:
    name: build ${{ matrix.name }} (${{ inputs.environment || 'ci' }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: saas-backend
            dir: saas/backend
            check: npx eslint "{src,apps,libs,test}/**/*.ts"
          - name: saas-frontend
            dir: saas/frontend
            check: npx tsc --noEmit
          - name: admin-backend
            dir: admin/backend
            check: npx tsc --noEmit
          - name: admin-frontend
            dir: admin/frontend
            check: npx tsc --noEmit
    env:
      # prisma generate exige as vars resolvíveis; nenhum banco é acessado no CI
      DATABASE_URL: postgresql://ci:ci@localhost:5432/ci
      DIRECT_URL: postgresql://ci:ci@localhost:5432/ci
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22.x
          cache: npm
          cache-dependency-path: ${{ matrix.dir }}/package-lock.json

      - name: Instalar dependências
        run: npm ci
        working-directory: ${{ matrix.dir }}

      - name: Build (mesmo comando do Heroku)
        run: npm run heroku-postbuild
        working-directory: ${{ matrix.dir }}

      # Informativo: a base ainda tem violações de lint/tipos herdadas.
      # Não bloqueia deploy; ver docs/decisions.md.
      - name: Lint / typecheck (informativo)
        continue-on-error: true
        run: ${{ matrix.check }}
        working-directory: ${{ matrix.dir }}

  schema-sync:
    name: prisma schema sync (admin)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22.x

      - name: Regenerar cópia do schema para admin/backend
        run: npm run admin:sync-schema

      - name: Falhar se admin/backend/prisma/schema.prisma estiver desatualizado
        run: |
          if ! git diff --quiet -- admin/backend/prisma/schema.prisma; then
            echo "::error file=admin/backend/prisma/schema.prisma::Schema do admin fora de sincronia. Rode 'npm run admin:sync-schema' e commite."
            git --no-pager diff -- admin/backend/prisma/schema.prisma
            exit 1
          fi
          echo "Schema do admin em dia."

  content-sync:
    name: docs e guides sincronizados (admin/frontend)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22.x

      - name: Verificar public/internal-docs e public/guides
        run: bash scripts/check-content-sync.sh
